apiVersion: v1
kind: Service
metadata:
  name: claudygod-web
  namespace: claudygod
  labels:
    app: claudygod-web
spec:
  type: ClusterIP
  selector:
    app: claudygod-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
